<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    android:id="@+id/container_payments_methods"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"

    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/background_payment"
    android:foreground="?android:attr/selectableItemBackground"
    android:padding="@dimen/padding_default"
    tools:background="?android:attr/colorPrimary">

    <ImageView
        android:id="@+id/payments_card_icon"
        android:layout_width="52dp"
        android:layout_height="32dp"
        tools:src="@drawable/icon_payment_card_visa"
        app:layout_constraintBottom_toTopOf="parent"
        app:layout_constraintTop_toBottomOf="parent"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/payments_card_number"
        style="@style/Text.Bold.Large"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        tools:textColor="@color/color_white"
        app:layout_constraintBottom_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@id/payments_card_icon"
        app:layout_constraintTop_toBottomOf="parent"
        tools:text="****  1234" />

    <ImageView
        android:id="@+id/payments_card_selected"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_check_accent_24dp"
        android:visibility="gone"
        tools:visibility="visible"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="parent"
        app:layout_constraintTop_toBottomOf="parent"/>

</android.support.constraint.ConstraintLayout>